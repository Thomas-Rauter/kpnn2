# pip install -e ".[dev]"  # when the pin or env changed
python -m ruff format .
python -m ruff check . --fix
python -m ruff format --check .
python -m mypy src
# python -m pytest --fast-only
python -m pytest
